# 버전은 == 고정 (CLAUDE.md 절대 규칙)
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic-settings==2.15.0
email-validator==2.3.0

sqlalchemy==2.0.52
asyncpg==0.31.0
# 스키마 변경 수단. 배포 이미지 안에서도 돌므로 테스트 도구가 아니라 런타임 의존이다 (ADR-0007)
alembic==1.19.1

pyjwt==2.13.0
pwdlib[argon2]==0.3.1

# 캐시·락·큐 1인 3역 (ADR-4). 지금 쓰는 것은 캐시뿐이다
redis==8.1.0
# 세션 10~11부터 테스트 도구가 아니라 런타임 의존이다 — 외부 API를 이걸로 부른다
httpx==0.28.1

pytest==9.1.1
pytest-asyncio==1.4.0
