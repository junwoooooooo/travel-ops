# Travel Ops — 여행 운영 시스템 개발 로드맵

> AI 백엔드 신입 취업 포트폴리오 프로젝트.
> "일정 뽑아주는 챗봇"이 아니라, **계획하고(Plan) → 실행하고(Execute) → 여행 중에도 스스로 감시·대응하는(Operate) 여행 운영 시스템**.
> 진입점이 두 개다: 사용자용 HTTP 입구(Nginx 경유)와 기계용 입구(Celery Beat 경유). 이 두 번째 입구가 챗봇과의 차이다.

- 기간: 10주 (풀타임 기준. 병행 시 1.5배로 환산)
- 목표: 국내 AI 백엔드 채용 공고 요구 역량을 실측 숫자와 함께 증명하는 배포된 서비스

---

## 1. 요구사항·제약 (모든 설계 결정의 근거)

| ID | 내용 |
|----|------|
| R1 | LLM이라는 느리고(수십 초) 비결정적인 부품을 안정적으로 다뤄야 한다 |
| R2 | 사용자가 없어도 시스템이 스스로 감시하고 행동해야 한다 (서비스 정체성) |
| R3 | 개발자 1명, 10주, 소규모 트래픽, 운영 인력 0명 |
| R4 | 데이터는 관계가 뚜렷하고(회원-여행-블록), 틀리면 안 되는 제약(예산·시간)이 있다 |
| R5 | 목적이 취업 포트폴리오다 — 공고 요구 기술 증명 + 면접 시연 가능해야 한다 |

모든 설계 근거는 R1~R5 중 하나를 가리켜야 한다. 화살표가 안 그려지면 유행 추종이다.

---

## 2. 서비스 기능 (사용자 5 + 관리자 1)

**사용자 서비스**
1. **대화형 일정 생성** — 부족한 정보는 되묻고(human-in-the-loop), 진행 상황을 SSE 스트리밍으로 중계
2. **조건 보장 배지** — 예산·운영시간·이동시간을 코드/솔버로 검증, "검사받은 일정" 표시
3. **부분 수정** — "2일차만 바꿔줘" → 해당 부분만 재생성, 나머지 보존
4. **승인 기반 모의 예약** (옵션/v2) — 예산 위임장 → 장바구니 → 사용자 최종 승인
5. **자동 감시·선제 알림** — 확정 후 항공·날씨·장소 영업정보 감시, 이상 시 수정 제안 알림. 돈 드는 변경은 승인 대기

**관리자 대시보드** (사용자 화면과 분리)
- 요청별 비용·지연 명세(AI 영수증), p50/p95, 툴 실패율, 평가 점수 추이, 카오스 토글(장애 주입)

**여행 중 대응 원칙**
- 계획은 명령이 아니라 제안. 이탈(늦잠·변심·폐업)은 정상 플로우
- Plan B(대체지 2곳)는 생성 시점에 사전 계산해 블록에 저장 — 현장 지연 0초
- 지연 시 전체 재생성이 아니라 압축: filler 탈락시켜 anchor 보존 (규칙 기반, 0원)
- 예약은 대행하지 않고 관리한다: 예약 필요 라벨 + D-1 리마인더

---

## 3. 아키텍처 결정 요약 (ADR)

각 결정은 `docs/adr/`에 파일로 기록한다. 형식: 상황 / 후보 / 결정과 근거 / 뒤집는 조건.

| # | 결정 | 선택 | 핵심 근거 | 뒤집는 조건 |
|---|------|------|-----------|------------|
| 1 | 구조 크기 | 모놀리스 + 워커 프로세스 분리 | MSA는 조직 기술, 1인에겐 비용만(R3). 모듈 경계는 미리 긋기 | 팀 확장·부분 트래픽 폭증 |
| 2 | 언어·프레임워크 | Python + FastAPI | 비동기 기본(R1), AI 백엔드 공고 표준(R5), Pydantic·Swagger 내장 | 전통 Java/Spring 시장 타깃 변경 |
| 3 | 주 저장소 | PostgreSQL + PGVector | 관계·정합성(R4), RAG 임베딩까지 한 집(R3) | 임베딩 수백만 건 규모 |
| 4 | 보조·브로커 | Redis 1인 3역(캐시·락·큐) | 부품 최소화(R3), Celery 브로커 교체 출구 열어둠 | 메시지 유실이 돈 문제가 될 때 → RabbitMQ |
| 5 | 백그라운드 | Celery 워커 + Beat, 별도 프로세스 | 감시는 정체성(R2), 유실 방지·재시도·독립 확장 | 작업 단순화 시 크론 다운그레이드 |
| 6 | 오케스트레이션 | LangGraph | 실패 지점 가시화·체크포인트(R1), 공고 키워드(R5). 노드 로직은 순수 함수로 | 흐름이 2단계로 단순해지면 제거 |
| 7 | 검증 | 규칙·솔버 기반 (LLM 자체검증 금지) | LLM은 제약 계산에 약함(TravelPlanner: GPT-4 통과 0.6%). 말은 LLM, 계산은 코드 | 없음(헌법 조항) |
| 8 | 실시간 전달 | SSE | 단방향이면 충분, HTTP 그대로(R1). Nginx `proxy_buffering off` 필수 | 양방향 조작 필요 시 WebSocket |
| 9 | 인증 | JWT (단기 만료) | 무상태·다중 클라이언트. 세션과 트레이드오프 설명 가능해야 | 즉시 무효화 요구 → 세션/블랙리스트 |
| 10 | 배포 | EC2 + RDS + Nginx + Docker Compose | 한국 면접 공용어(R5), 인바운드·웹서버 직접 경험. K8s는 R3 위반 | 실서비스 전환 시 PaaS 회귀 합리적 |
| + | 예약 | mock (모의) | 실재고 API 접근 불가, 책임 경계. AP2 스타일 승인 게이트는 v2 |  |

---

## 4. 디렉토리 구조

```
travel-ops/
├── docker-compose.yml        # api·worker·beat·postgres·redis·nginx 조립도
├── Dockerfile                # 레이어 캐시 순서: requirements 먼저, 코드 마지막
├── requirements.txt          # 버전 == 고정
├── .env                      # 비밀. 커밋 금지 (.env.example만 커밋)
├── .github/workflows/ci.yml  # 테스트 + (Phase 4부터) 평가 게이트 + CD
├── docs/adr/                 # 설계 결정 기록
├── evals/                    # 골든셋 + 채점기
├── tests/
└── app/
    ├── main.py               # 도메인 입구 등록만
    ├── config.py             # pydantic-settings, fail fast
    ├── core/                 # 설비: db.py, cache.py, security.py, llm.py
    ├── integrations/         # 외부 API 툴 레이어: kakao.py, weather.py, flights.py
    ├── auth/                 # [도메인] 회원·로그인
    ├── trips/                # [도메인] 여행 보관·조회 (금고)
    ├── planning/             # [도메인] 계획 생성: graph.py, solver.py, rag.py
    ├── monitoring/           # [도메인] 감시: tasks.py, watch.py
    └── notifications/        # [도메인] 알림
```

**규칙**
1. 도메인 = 4파일 문법(router / service / models / schemas). router는 얇게, service는 두껍게
2. 도메인 간 접근은 상대 service 함수로만. 남의 models(테이블) 직접 접근 금지
3. 의존 방향: 도메인 → core/integrations 만 허용. 역방향 금지
4. 폴더는 해당 Phase 도착 시 생성 (빈 폴더 미리 만들지 않음)

**핵심 스키마 (Block)** — Phase 1에서 필드 자리를 미리 판다:
```python
class Block:
    place: Place                              # place_id 필수 = 실존 검증(환각 차단)
    priority: Literal["anchor", "filler"]     # 지연 시 지킬 것 vs 버릴 것
    booking: Literal["none", "required"]
    booking_confirmed: bool = False
    alternates: list[Place] = []              # Plan B 2곳 (사전 계산)
    verified_at: datetime                     # 영업정보 확인 시각
    slack_min: int = 0                        # 여유시간
```

---

## 5. 전체 로드맵

### Phase 0 · 1주차 — 걷는 뼈대 (환경 + auth)
> DoD: **git push 하면 30초 뒤 실제 URL에 반영된다**

- [x] **세션 1 — 연장**: Python 3.13, Docker Desktop, Git, VS Code 설치. 버전 명령 3개 확인
- [ ] **세션 2 — 리포**: GitHub 리포 생성 → clone → **`.gitignore` 최우선 작성**(.env, __pycache__/, venv/) → 뼈대 12파일 작성(requirements, .env, config, core/db, core/security, auth 4파일, main, Dockerfile, compose) → login을 OAuth2PasswordRequestForm(폼) 버전으로 → 첫 커밋·푸시. 확인: GitHub에서 .env만 안 보임
- [ ] **세션 3 — 첫 가동**: `docker compose up --build` → `localhost:8000/docs` → Swagger 풀코스: 가입(201) → 중복(409) → Authorize 로그인 → /me(200) → 로그아웃 후 /me(401)
- [ ] **세션 4 — 하이브리드 전환**: postgres에 `ports: ["5432:5432"]`, .env는 `@localhost`, compose api에는 `environment:`로 `@postgres` 직접 지정. venv 생성·설치 → `docker compose up postgres redis` + `uvicorn --reload`. 저장 즉시 반영 체험
- [ ] **세션 5 — 시험지+CI**: pytest·httpx 설치, tests/test_auth.py(가입→로그인→/me 왕복), 로컬 통과 → ci.yml(push·PR 트리거, postgres service, pytest) → Actions 초록불
- [ ] **세션 6 — EC2+RDS**: 프리티어 EC2 + RDS 생성, 인바운드 규칙 설정, 서버에 .env 작성, Nginx 컨테이너 추가(SSE 경로 `proxy_buffering off`), 외부 IP 접속 확인. **결제 알림(Billing Alert) 설정. 실습용 인스턴스는 사용 후 삭제**
- [ ] **세션 7 — CD**: GitHub Secrets(EC2_HOST, EC2_SSH_KEY) → deploy job(`needs: test`, main만) → push → 자동 배포 확인

### Phase 1 · 2~3주차 — 일정이 나온다
> DoD: **"오사카 3박4일 미식, 150만원" 입력 → place_id 달린 실존 장소 일정이 스트리밍으로 출력**
> 숫자: TTFT / 툴 순차 vs 병렬 지연 비교

- [ ] 세션 8~9 — trips 도메인 4파일 (auth 베끼며 직접). Block 스키마 필드 전부 미리. ERD: users 1:N trips 1:N blocks. 인가: 내 여행만(없는 것과 남의 것 동일 응답)
- [ ] 세션 10~11 — integrations: kakao(장소), weather. async httpx + 타임아웃 + 재시도 + Redis 캐시. 장애 격리: 날씨 API 죽어도 일정은 나옴
- [ ] 세션 12~14 — planning: Pydantic 강제 파싱 + 되묻기 분기 → graph(parse→research→draft) → place_id 강제 → SSE 스트리밍("날씨 확인 중…")

### Phase 2 · 4~5주차 — 믿을 수 있는 일정
> DoD: **검증 배지가 뜨고, "2일차만 바꿔줘"가 2일차만 바꾸고, 모든 블록에 대안 2곳이 붙어 있다**
> 숫자: 검증 통과율 / 부분 수정 vs 전체 재생성 토큰 비용(→ 절감률 스토리 발원지)

- [ ] validate 노드(예산 합계·운영시간·이동시간 — 전부 코드 검증) + refine 루프(상한 3회, revision_count)
- [ ] 검증 배지 응답 노출 (기능 ②)
- [ ] 부분 수정 API `POST /trips/{id}/revise` (기능 ③)
- [ ] Plan B 사전 계산(같은 카테고리·도보권·비슷한 가격대 2곳) + anchor/filler 부여 + slack 심기
- [ ] DB 근육: SQLAlchemy `echo=True`로 로우 쿼리 확인 습관, 조인/통계 쿼리 1개+(일자별 비용 합계 등), 인덱스 before/after 수치(blocks.trip_id)
- 참고: 솔버(OR-Tools)는 "규칙 검증"으로 시작. 완전 제약 배치는 여유 시 심화

### Phase 3 · 6주차 — RAG 근거 인용
> DoD: **장소마다 출처 달린 근거 1~3건이 접힘 섹션으로 붙는다**
> 숫자: 근거 인용률 / 검색 정확도(질문 20개 셋)

- [ ] 데이터: 저작권 안전 소스(한국관광공사 TourAPI 등 공공데이터) 수집
- [ ] 청킹 → 임베딩 → PGVector 적재
- [ ] graph에 retrieve 노드 추가: draft가 장소 고를 때 근거 검색
- [ ] 이 주로 RAG·벡터DB·임베딩 공고 키워드 확보 → 지원 가능 공고 범위 확대

### Phase 4 · 7주차 — 시험과 계기판
> DoD: **PR마다 자동 점수 리포트. 대시보드가 실데이터 표시. 이후 모든 개선의 before 확정**

- [ ] 골든셋 20~30건 (예: "부산 2박3일 30만원")
- [ ] 2단 채점: 규칙(예산 초과·날짜 수·운영시간 충돌) + LLM-as-a-judge(취향 반영·동선)
- [ ] CI 회귀 게이트: 점수 하락 시 머지 차단
- [ ] 관측성: 요청별 토큰 비용·스텝별 지연 DB 기록, `/metrics`
- [ ] 관리자 대시보드 v1: p50/p95, 요청당 비용, 평가 점수 추이

**★ 지원 시작선 — 여기부터 이력서 제출과 병행. "현재 v2 개발 중"은 면접 소재가 된다**

### Phase 5 · 8~9주차 — 살아있는 서비스 (챗봇 졸업)
> DoD: **관리자 화면 "2일차 폭우 주입" 버튼 → 사람 개입 0으로 감지→판단→부분 재계획→알림 완주** (데모 영상 하이라이트)
> 숫자: 감지→알림 리드타임 / LLM 없이(0원) 처리된 이벤트 비율

- [ ] watches 테이블 + 계획 확정 시 감시 등록(여행 종료일 자동 만료)
- [ ] Celery Beat 30분 폴링 → 스냅샷 저장 → diff(상태 전이 감지)
- [ ] 2단 판단: 룰 필터(공짜) → LLM 영향 평가(유료)
- [ ] 멱등성(이벤트 ID + 처리 기록) + Redis 락(여행 단위 동시 재계획 방지)
- [ ] 압축 엔진: 지연 시 filler 탈락으로 anchor 보존(규칙 기반)
- [ ] 이벤트 API 3종: 완료/건너뜀/문닫음 → 단일 이벤트 파이프라인 수렴(버튼·채팅·감시자 3채널)
- [ ] 무인 실행 가드레일: 자율 행동 2등급(정보 갱신=자동, 돈 걸린 변경=승인 대기), 토큰 예산 상한, 감사 로그
- [ ] 알림: 디스코드 웹훅 → 알림에서 대화 복귀 링크
- [ ] 당일 모드: 버튼 3개짜리 반응형 웹 1페이지 (또는 채팅 인텐트로 대체)
- [ ] D-1 장소 영업정보 일괄 재검증 + 예약 미확인 리마인더

### Phase 6 · 10주차 — 포장
> DoD: **링크 하나로 처음 보는 사람이 5분 안에 프로젝트를 이해한다**

- [ ] 카오스 토글 마감(외부 API 죽이기 라이브 시연)
- [ ] README 최상단: 공고 요구사항 매핑표(출처 명시) + 아키텍처 다이어그램 2장 + 핵심 숫자
- [ ] README 본문: 설계 결정 기록(폐업·지연·변심 대응 설계 과정) / 말미: v2 로드맵
- [ ] 3분 데모 영상 (지민 시나리오 순서: 생성→배지→수정→확정→폭우 알림→당일 모드)
- [ ] PDF 이력서 + 노션 포트폴리오 반영, 4질문 카드 12장 최종 점검

---

## 6. 스코프 규칙

**시간 부족 시 자르는 순서 (미리 정해둔 것)**
1. 당일 모드 UI → 채팅 인텐트로 대체
2. RAG → 근거 인용 없이 검색만으로 축소
3. 솔버 심화(OR-Tools 완전 배치) 포기 → 규칙 검증 유지
4. 알림 → 웹훅 하나로

**여유 시 추가하는 순서**
1. OR-Tools 완전 제약 배치 (+미니 벤치마크: LLM 단독 vs 하이브리드 통과율)
2. 플래너를 MCP 서버로 노출 (반나절)
3. 모델 라우터(쉬운 요청=저가 모델) + 비용 절감 수치
4. AP2 스타일 승인 게이트(Intent/Cart 위임장), 프롬프트 인젝션 방어 시연

**어떤 경우에도 못 자르는 것**: 배포 상태 유지 / 평가 파이프라인 / 숫자 기록

---

## 7. 측정 지표 (README에 박을 숫자)

| 지표 | 어디서 | 목표 서사 |
|------|--------|----------|
| TTFT (첫 응답까지) | SSE | "30초 침묵 → 0.5초 첫 반응" |
| p50/p95 응답 | /metrics | 평균의 함정 회피 |
| 부분 재계획 절감률 | Phase 2 | "전체 재생성 대비 토큰 N% 절감" |
| 캐시 적중률 | Redis | 아낀 외부 호출 비용 |
| 검증 통과율·걸러낸 위반 | validate | "검사받은 일정" |
| 골든셋 점수 | evals | 회귀 차단 이력 |
| 감지→알림 리드타임 | Phase 5 | 무인 대응 속도 |
| 0원 처리 이벤트 비율 | 압축 엔진 | 비용 설계 관점 |
| 인덱스 before/after | DB | "120ms→8ms" |

---

## 8. Claude Code 작업 지침

1. **한 번에 한 세션만.** 현재 위치는 체크박스로 관리. 세션 시작 시 이 파일에서 해당 세션만 읽고 작업
2. **세션 종료 조건 = DoD 확인 + 테스트 통과 + 커밋.** main은 항상 배포 가능 상태 유지(작업은 브랜치 → PR)
3. **디렉토리 규칙(4장) 위반 금지**: 도메인 간 직접 models 접근 금지, router 얇게, 의존 방향 준수
4. **비밀은 .env만.** 코드·로그·커밋에 비밀값 금지. .env.example 최신 유지
5. **측정 먼저**: 최적화 전에 반드시 before 수치를 기록하고 시작
6. **새 아이디어는 구현하지 말고 6장 백로그에 추가** (스코프 방어)
7. **파일 생성 최소주의**: 해당 Phase에 필요한 폴더·파일만 생성. 미래 대비 빈 파일 금지
8. 라이브러리 추가 시 requirements.txt에 == 버전 고정으로 기록
9. LLM 노드 로직은 프레임워크 비종속 순수 함수로 작성 (LangGraph 교체 가능성)
10. 에러 응답 표준화: 검증 422, 인증 401, 소유권 위반은 404(존재 노출 금지), 중복 409
